import React from 'react';
import BaseParagraph from '../../../common/base-paragraph/BaseParagraph';

const ExpiredNotice = () => {
  return (
    <BaseParagraph className={'mb-0'} text={'SLA süresi geçmiş'}/>
  );
};

export default ExpiredNotice;
